tools:
    php_changetracking:
        enabled: true
    php_code_coverage:
        enabled: true
    php_sim:
        enabled: true
    php_code_sniffer:
        enabled: true
        config:
            standard: "PSR2"
    php_cpd:
        enabled: true
    php_cs_fixer:
        enabled: false
    php_mess_detector:
        enabled: true
    php_pdepend:
        enabled: true
    php_analyzer:
        enabled: true
#    external_code_coverage:
#        timeout: 600
